src/
├── app/
│   ├── layout.tsx              ← RTL + Cairo font
│   ├── globals.css             ← design tokens
│   ├── page.tsx                ← redirect to /login
│   ├── (auth)/
│   │   ├── layout.tsx
│   │   ├── login/page.tsx
│   │   ├── register/page.tsx
│   │   └── forgot-password/page.tsx
│   ├── (dashboard)/
│   │   ├── layout.tsx          ← Sidebar + main
│   │   ├── student/page.tsx
│   │   ├── teacher/page.tsx
│   │   └── parent/page.tsx
│   └── courses/page.tsx
│
├── features/
│   ├── auth/                   ← types + validation + service + hook + components
│   ├── courses/                ← types + store (zustand) + service + hook
│   └── assistants/             ← service + hook
│
├── components/
│   ├── ui/                     ← Button, Input, Card, Badge, Spinner
│   └── common/                 ← Sidebar, AuthLayout
│
├── services/apiClient.ts       ← axios + interceptors
├── store/authStore.ts          ← zustand + persist
├── hooks/                      ← useDebounce, useMediaQuery
└── utils/                      ← cn(), formatDate()